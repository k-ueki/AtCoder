#include <bits/stdc++.h>
#define rep(i,n) for(int i=0;i<(n);i++)
#define fir first
#define sec second
typedef long long ll;
typedef pair<int,int> P;

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    int n,m; cin >> n >> m;
    vector<P> p(m);
    rep(i,m)cin >> p[i].fir >> p[i].sec;

    rep(x,1000){
        int keta = 1;
        int nx = x/10;
    }

    return 0;
}