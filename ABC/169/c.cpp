#include <bits/stdc++.h>
#define rep(i,n) for(int i=0;i<(n);i++)
typedef long long ll;

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    ll a;double b; cin >> a >> b;
    ll ans = (a*b*100)/100;
    cout << ans << endl;

    return 0;
}